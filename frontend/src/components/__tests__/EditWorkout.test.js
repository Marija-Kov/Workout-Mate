
describe("<EditWorkout/>", () => {
  it("should render Edit workout form given that user is authenticated", () => {
    expect(true).toBe(false);
  });

  it("should focus input fields in the right order", () => {
    expect(true).toBe(false);
  });

  it("should update input value when user types", () => {
    expect(true).toBe(false);
  });

  it("should respond with error message when user attempts to submit edit form with invalid input value(s)", () => {
    expect(true).toBe(false);
  });

  it("should respond with error message if authentication token expired and user attempts to submit", () => {
    expect(true).toBe(false);
  })

  it("should close Edit workout form when user clicks on close button", () => {
    expect(true).toBe(false);
  });

  it("should close Edit workout form and render updated workout given that all input values are valid", () => {
    expect(true).toBe(false);
  });
});

describe("<UserSettings/>", () => {
    it("should render UserSettings component correctly", () => {
     expect(true).toBe(false);
    });

    it("should focus elements in correct order", () => {
     expect(true).toBe(false);
    });

    it("should update new displayed name input value when user types and enable upload button", () => {
     expect(true).toBe(false);
    });

    it("should show error message and disable upload button if name is too long", () => {
     expect(true).toBe(false);
    })

    it("should respond with error message and disable upload button if new username is too long", () => {
     expect(true).toBe(false);
    });

    it("should respond with success message and show new name and avatar in Navbar if profile was updated successfully", () => {
     expect(true).toBe(false);
    });

    it("should render delete account dialogue component when 'delete account' button is clicked", () => {
     expect(true).toBe(false);
    });

});
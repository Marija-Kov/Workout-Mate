describe("<WorkoutForm/>", () => {
  it("should render Workout form given that user is authenticated", () => {
    expect(true).toBe(false);
  });

  it("should focus input fields in the right order", () => {
    expect(true).toBe(false);
  });

  it("should update input value when user types", () => {
    expect(true).toBe(false);
  });

  it("should respond with error message when user attempts to submit form with invalid input value(s)", () => {
    expect(true).toBe(false);
  });

  it("should respond with error message if authentication token expired and user attempts to submit", () => {
    expect(true).toBe(false);
  });

  it("should close Workout form when user clicks on close button", () => {
    expect(true).toBe(false);
  });

  it("should close Workout form, flip to page 1 of workout results and render new workout in the list of workouts given all input values are valid", () => {
    expect(true).toBe(false);
  });
});
